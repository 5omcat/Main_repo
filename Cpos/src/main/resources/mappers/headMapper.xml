<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="HeadMapper">
<insert id="regist" parameterType="HeadVO">
 insert into tbl_head(pname,expire_term,get_price,sell_price,discount_rate,status,barcode,category) 
 values(#{pname},#{expire_term},#{get_price},#{sell_price},#{discount_rate},0,#{barcode},#{category})
</insert>
<select id="plist" parameterType="Criterion" resultType="HeadVO">
 <![CDATA[
 select barcode,pname,category from (select /*+INDEX_DESC(tbl_head category)*/
 rownum as rn,barcode,pname,category
 from tbl_head
 where rownum <= #{pageNum}*#{amount})
 where rn > (#{pageNum}-1)*#{amount}]]> 
</select>
<select id="total" resultType="java.lang.Integer">
 select count(barcode) from tbl_head
</select>
<!-- <select id="checkbarcode" parameterType="java.lang.Integer" resultType="java.lang.Integer"></select>
=======
<insert id="regist" parameterType="HeadVO"></insert>
<select id="checkbarcode" parameterType="java.lang.Integer" resultType="java.lang.Integer"></select>
>>>>>>> abe9930cf14edf98eaa6dcd090e093663fdb0507
<select id="checkpname" parameterType="java.lang.String" resultType="java.lang.Integer"></select>

<select id="lcate" parameterType="Criterion" resultType="HeadVO"></select>
<select id="mcate" parameterType="Criterion" resultType="HeadVO"></select>
<update id="modify" parameterType="HeadVO"></update>
<delete id="remove" parameterType="java.lang.Integer"></delete>
 -->
</mapper>